its a e-commarce project
we'll user
- Kraken ( we can't use that cause we can't generate the app right now)
-so let's use express (by the way Kraken is also using express)
-MongoDb
-Mongoose
-Dust template which is dafault of Kraken
-Maybe Bootstrap for front end